# target-agent

The naive customer-support agent ChaosLab attacks. Three tools (`lookup_order`, `refund`, `escalate`), weak prompt, no input validation — by design.

Real implementation lands in Epic 2 (S2.1-S2.4). Currently a workspace-member skeleton per S1.1.
