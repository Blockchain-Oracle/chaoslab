// localStorage preference keys — shared constants so a rename can't silently
// disconnect the settings page from the surfaces that read them (the exact
// "decorative preference" bug story-9.10 fixed).

export const PREF_HOSTING = 'pa_hosting'
export const PREF_FRAMEWORK = 'pa_framework'
