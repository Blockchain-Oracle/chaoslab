# chaoslab-agent

The ChaosLab orchestrator — Python ADK service. SequentialAgent composing Injector + Judge + Patcher sub-agents. A2A peer connection to the target.

Real implementation lands in Epic 4 (stories 4.1-4.6). This directory currently holds only the workspace-member skeleton per S1.1.

See `docs/architecture.md` + `docs/stories/story-4.*` for details.
